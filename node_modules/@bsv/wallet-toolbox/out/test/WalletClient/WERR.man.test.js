"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const sdk_1 = require("@bsv/sdk");
const types_1 = require("../../src/sdk/types");
const WalletError_1 = require("../../src/sdk/WalletError");
test('0 WERR_REVIEW_ACTIONS', async () => {
    const wallet = new sdk_1.WalletClient(undefined, '0.WERR.man.test');
    try {
        const r = await wallet.createAction({
            labels: [types_1.specOpThrowReviewActions],
            description: 'must throw'
        });
        expect(true).toBe(false);
    }
    catch (eu) {
        const e = WalletError_1.WalletError.fromUnknown(eu);
        expect(e.code).toBe('WERR_REVIEW_ACTIONS');
        expect(e.reviewActionResults).toBeTruthy();
    }
});
//# sourceMappingURL=WERR.man.test.js.map