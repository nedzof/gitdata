import express from 'express';
import http from 'http';
export declare class LocalCdnServer {
    port: number;
    folder: string;
    app: express.Application;
    server?: http.Server;
    constructor(port: number, folder: string);
    start(): Promise<void>;
    stop(): Promise<void>;
}
//# sourceMappingURL=LocalCdnServer.d.ts.map